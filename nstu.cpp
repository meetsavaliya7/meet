#include <iostream>
using namespace std;
class skill
{
   int mathe,guj,eng,total,par;
public:
    #include <iostream>
using namespace std;
class skill
{
    int maths, phy, chem, total, per, x;

public:
    skill()
    {
        22
        cout << "\nENTER THE  : ";
        cin >> maths;

        cout << "\nENTER THE  : ";
        cin >> chem;

        cout << "\nENTER THE  : ";
        cin >> phy;

        total = maths + chem + phy;
        cout << "\ntotal  : " << total;

        per = total / 3;
        cout << "\nper : " << per;

        if (per >= 91 && per <= 100)
        {
            cout << "\nA1";
        }
        else if (per >= 81 && per <= 90)
        {
            cout << "\nA2";
        }
        else if (per >= 71 && per <= 80)
        {
            cout << "\nB2";
        }
        else if (per >= 61 && per <= 70)
        {
            cout << "\nC1";
        }
        else if (per >= 51 && per <= 60)
        {
            cout << "\nC2";
        }
        else if (per >= 41 && per <= 50)
        {
            cout << "\nD";
        }
        else if (per >= 31 && per <= 40)
        {
            cout << "\nE1";
        }
        else if (per >= 21 && per <= 30)
        {
            cout << "\nD";
        }
        else if (per >= 11 && per <= 20)
        {
            cout << "\nFAIL";
        }
    }
    skill(int n)
    {
        x = n;
        cout << "\nENTER THE  : ";
        cin >> maths;

        cout << "\nENTER THE  : ";
        cin >> chem;

        cout << "\nENTER THE  : ";
        cin >> phy;

        total = maths + chem + phy;
        cout << "\ntotal  : " << total;

        per = total / 3;
        cout << "\nper : " << per;

        if (per >= 91 && per <= 100)
        {
            cout << "\nA1";
        }
        else if (per >= 81 && per <= 90)
        {
            cout << "\nA2";
        }
        else if (per >= 71 && per <= 80)
        {
            cout << "\nB2";
        }
        else if (per >= 61 && per <= 70)
        {
            cout << "\nC1";
        }
        else if (per >= 51 && per <= 60)
        {
            cout << "\nC2";
        }
        else if (per >= 41 && per <= 50)
        {
            cout << "\nD";
        }
        else if (per >= 31 && per <= 40)
        {
            cout << "\nE1";
        }
        else if (per >= 21 && per <= 30)
        {
            cout << "\nD";
        }
        else if (per >= 11 && per <= 20)
        {
            cout << "\nFAIL";
        }
    }
};
int main()
{
    skill s1[3];
}


   
    {
        for (i = 0; i < 5; i++)
        {
            cout << "Enter the ";
            cin >> roll[i];
            cout << "Enter the maths ";
            cin >> maths[i];
            cout << "Enter the chem";
            cin >> chem[i];
            cout << "Enter the phy";
            cin >> phy[i];

            total[i] = maths[i] + chem[i] + phy[i];
            cout << "\n total : " << total[i];

            per[i] = total[i] / 3;
            cout << "\n per : " << per[i];

            if (per[i] >= 91 && per[i] <= 100)
            {
                cout << "A1";
            }
            else if (per[i] >= 81 && per[i] <= 90)
            {
                cout << "A2";
            }
            else if (per[i] >= 71 && per[i] <= 80)
            {
                cout << "B2";
            }
            else if (per[i] >= 61 && per[i] <= 70)
            {
                cout << "C1";
            }
            else if (per[i] >= 51 && per[i] <= 60)
            {
                cout << "C2";
            }
            else if (per[i] >= 41 && per[i] <= 50)
            {
                cout << "D";
            }
            else if (per[i] >= 31 && per[i] <= 40)
            {
                cout << "E1";
            }
            else if (per[i] >= 21 && per[i] <= 30)
            {
                cout << "D";
            }
            else if (per[i] >= 11 && per[i] <= 20)
            {
                cout << "FAIL";
            }
        }
    }
};
int main()
{
    skill s1;
    s1.getdata();
}
