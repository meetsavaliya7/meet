#include<stdio.h>
#include<stbile.h>
iunt main ()
{
	int a[10],i,sum=0;
	for(i=0;i<5;i++)
	{
		printf("enter value a[0]",i);
		scanf("%d",&a[i]);
	}
	for(i=0;i<5;i++)
	{
		sum=a[i]+a[i+1]
		printf("\t sum of all is %d",sum);
	}
}